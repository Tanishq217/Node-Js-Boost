// Executing first JS code
console.log("Hello I am Tanishq")

// MODULE 
// const fs = require('fs');

// fs.writeFile("Output.text", "Writing File " , (err) =>{
//   if(err) console.log("Error Occured !") ; 
//   else {
//     console.log("File Created Successfully");
//   }
// })


// STARTING WITH BACKEND // 

// REQUIRE KEYWORD // 
// import and export are used in ES6 modules, while require is used in CommonJS modules. Caching is a feature of both module systems, but they handle it differently. In CommonJS, modules are cached after the first time they are loaded, so subsequent calls to require return the cached version. In ES6 modules, caching is also done, but the syntax and behavior are different.

// const firstMOdule = require('module'); // Importing firstModule.js

// 